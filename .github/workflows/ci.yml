name: CI

on:
  push:
    branches: ["master", "main", "feature/*", "fix/*"]
  pull_request:
    branches: ["master", "main"]

jobs:
  test:
    uses: Chao-hu-Lab/shared-workflows/.github/workflows/python-ci.yml@c5276382aafc2ee6c0f14b3651cad8aca9f74758
    with:
      python-versions: '["3.11", "3.12"]'
      install-args: "-e .[dev]"
      pythonpath: ""
      submodules: false
